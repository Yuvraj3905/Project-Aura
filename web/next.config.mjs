/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The custom server (server.ts) handles HTTP + WebSocket; Next runs in its handler.
};

export default nextConfig;
